@model GetByIdProductDetailDto

<div class="tab-pane fade show active" id="tab-pane-1">
    <h4 class="mb-3">Ürün Detayı</h4>
    <p><p>@(Model?.ProductDetailId != null ? Model.ProductInfo : "Ürün Detayı Girilmedi.")</p></p>
</div>